#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -z "$NODE_PATH" ]; then
  export NODE_PATH="$basedir/C:/Users/win_n/Desktop/yizhi-word/node_modules/.store/pacote@12.0.3/node_modules"
else
  export NODE_PATH="$NODE_PATH:$basedir/C:/Users/win_n/Desktop/yizhi-word/node_modules/.store/pacote@12.0.3/node_modules"
fi
if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../../../pacote@12.0.3/node_modules/pacote/lib/bin.js" "$@"
else
  exec node  "$basedir/../../../../../../pacote@12.0.3/node_modules/pacote/lib/bin.js" "$@"
fi
